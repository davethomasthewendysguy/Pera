jQuery(document).ready(function($) {
	//Create the dropdown base
	/*$("<select />").appendTo("#navigation .menu-header");

	// Create default option "Navigation..."
	$("<option />", {
	   "selected": "selected",
	   "value"   : "",
	   "text"    : "-Navigation-"
	}).appendTo("#navigation .menu-header select");

	// Populate dropdown with menu items
	$("#navigation .menu-header a").each(function() {
		var el = $(this);
		$("<option />", {
			"value"   : el.attr("href"),
			"text"    : el.text()
		}).appendTo("#navigation .menu-header select");
	});

	$("#navigation .menu-header select").change(function() {
		window.location = $(this).find("option:selected").val();
	});*/
	
	
});